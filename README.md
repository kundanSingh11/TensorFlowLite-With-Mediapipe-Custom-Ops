# TensorFlowLite-With-Mediapipe-Custom-Ops
This Build contains tflite with custom ops from mediapipe this can be used to run mediapipe models with tflite.
Currently only contains Android build(AAR) files
